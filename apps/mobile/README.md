# The Guild — iPhone app

Expo (React Native) parent app. See [docs/MOBILE_APP.md](../../docs/MOBILE_APP.md) for setup, push, TestFlight, and backend migrations.

```bash
cp .env.example .env
npm install
npm start
```
