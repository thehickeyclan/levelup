import LoginScreen from '../(auth)/login';

/**
 * Guest-only "Login" tab: the branded sign-in / create-account page, shown as
 * the guest landing with the Training / Market / Login tab bar underneath.
 */
export default function JoinTab() {
  return <LoginScreen />;
}
