/** Minimum interactive target recommended by Apple for iPhone controls. */
export const MIN_TOUCH_TARGET = 44;

/**
 * Navigation and numeric badges have very limited space. Keep Dynamic Type on,
 * but cap those constrained labels so they do not overlap adjacent controls.
 * Reading content is intentionally left uncapped throughout the app.
 */
export const NAV_TEXT_MAX_SCALE = 1.5;
export const BADGE_TEXT_MAX_SCALE = 1.3;
