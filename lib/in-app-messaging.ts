/** Unified Guild Messages inbox (coaching + marketplace). */
export const IN_APP_MESSAGING_ENABLED = true;

export const MESSAGES_HOME_PATH = '/messages';

export const IN_APP_MESSAGING_PAUSED_MESSAGE =
  'Messaging is temporarily unavailable. Book or request a session from this profile, or text the coach when they have a number on file.';
